{
  "name": "@open-rush/control-worker",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/worker.ts --format esm --dts",
    "dev": "tsx watch --env-file-if-exists=.env.local src/worker.ts",
    "start": "node dist/worker.js",
    "check": "tsc --noEmit",
    "lint": "biome check .",
    "test": "vitest run --passWithNoTests",
    "clean": "rm -rf dist node_modules coverage"
  },
  "dependencies": {
    "@open-rush/contracts": "workspace:*",
    "@open-rush/control-plane": "workspace:*",
    "@open-rush/db": "workspace:*",
    "@open-rush/sandbox": "workspace:*",
    "@open-rush/stream": "workspace:*",
    "drizzle-orm": "^0.45.2",
    "pg-boss": "^12.15.0"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsup": "^8.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0",
    "vitest": "^4.0.0"
  }
}
